import Loading from "../../loading/Loading";

const OrganizerProfile = () => {
    return (
        <div>
            this is organizer profile
        </div>
    );
};

export default OrganizerProfile;